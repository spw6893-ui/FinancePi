{
  "$schema": "https://modelcontextprotocol.io/schema/mcp-client-config.schema.json",
  "description": "Example finance MCP config for user-provided free, self-hosted, or explicitly licensed MCP servers. Paid institutional endpoints are intentionally not listed.",
  "mcpServers": {
    "local-finance": {
      "type": "http",
      "url": "http://localhost:8765/mcp"
    },
    "custom-provider": {
      "type": "http",
      "url": "https://example.com/mcp",
      "headers": {
        "Authorization": "Bearer ${CUSTOM_FINANCE_MCP_TOKEN}"
      }
    }
  }
}
